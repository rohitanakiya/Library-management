#include "Include/Book_database.h"
#include "Include/User_Database.h"
#include "Include/login.h"
#include "Include/Initialization.h"
#include <iostream>
using namespace std;

int main(){

    initialization a;
    cout <<endl << "The portal is closing !!" <<endl <<endl;
    //login loginscrenn;
    /*
    user_database all_users_list;


    all_users_list.add();
    all_users_list.add();
    all_users_list.display();
    all_users_list.show_students();
    all_users_list.show_professors();
    all_users_list.show_librarians();


    all_users_list.update();
    all_users_list.display();
    all_users_list.show_students();
    all_users_list.show_professors();
    all_users_list.show_librarians();

    all_users_list.update();
    all_users_list.display();
    all_users_list.show_students();
    all_users_list.show_professors();
    all_users_list.show_librarians();

    all_users_list.remove();
    all_users_list.display();
    all_users_list.show_students();
    all_users_list.show_professors();
    all_users_list.show_librarians();
    */

    return(0);
}